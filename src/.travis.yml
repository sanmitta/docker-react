language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sanmitta/docker-react -f Dockerfile.dev .

script:
  - docker run sanmitta/docker-react npm run test -- --coverage
  
#docker run -e CI=true sanmitta/docker-react npm run test